basePath: /provisioner
consumes:
- application/json
definitions:
  ChefPayload:
    example:
      nodeAttributes: Ut eos non totam voluptatem.
      runlist:
      - Distinctio dolores.
      - Distinctio dolores.
      - Distinctio dolores.
      uid: Illo ipsum qui.
    properties:
      nodeAttributes:
        description: '{docker: {name: dockername}}'
        example: Ut eos non totam voluptatem.
        type: string
      runlist:
        example:
        - Distinctio dolores.
        - Distinctio dolores.
        - Distinctio dolores.
        items:
          example: Distinctio dolores.
          type: string
        type: array
      uid:
        description: kdielsie
        example: Illo ipsum qui.
        type: string
    title: ChefPayload
    type: object
host: localhost:3001
info:
  description: A teaser for goa
  title: The adder API
  version: ""
paths:
  /chef:
    post:
      operationId: chef#create
      parameters:
      - in: body
        name: payload
        required: true
        schema:
          $ref: '#/definitions/ChefPayload'
      responses:
        "200":
          description: OK
      schemes:
      - http
      summary: create chef
      tags:
      - chef
produces:
- application/json
schemes:
- http
swagger: "2.0"
